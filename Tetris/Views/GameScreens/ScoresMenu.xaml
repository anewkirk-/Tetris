<UserControl x:Class="Tetris.Views.GameScreens.ScoresMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel>
        <Label Content="Scores" />
        
        <!--<ItemsControl x:Name="SM_grid"
                      BorderBrush="Black"
                      BorderThickness="2"
                      Margin="50">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <UniformGrid Columns="2"
                                    SnapsToDevicePixels="True">
                        <Label Content="{Binding Path=Username}" />
                        <Label Content="{Binding Path=ScoreValue}" />
                    </UniformGrid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>-->
        
        <UniformGrid x:Name="SM_grid">
            
            
        </UniformGrid>
        
        <Button x:Name="SM_back"
                Style="{StaticResource ResourceKey=MenuButtons}"
                Content="Back to Main Menu" />
        
    </StackPanel>
</UserControl>
